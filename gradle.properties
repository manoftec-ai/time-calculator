# AndroidX configuration
android.useAndroidX=true
android.enableJetifier=false

# Gradle settings
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Disable aapt2 daemon (use direct aapt2 invocations)

# Try to disable aapt2 daemon
